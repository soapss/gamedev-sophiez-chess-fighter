name = "TidEscape"
instruction = "./instruction.md"

[game]
# Controlled vocabularies — see the "Tag reference" tables in this task's README.md.
game-tags = ["Arcade/action"]            # one or more from the game-tags list
tech-stack-tags = ["Unspecified/other"]  # raylib/C — no dedicated tag; see the list
assets-used = ["Primitives"]             # Primitives | ImportedAssets
avocado-model = "Muse Spark 1.1"         # exact Avocado model used for the gold build
harness = "Metacode"                     # harness used to build the gold solution
screenshots = ["./screenshots/screen-01.png"]
videos = ["./videos/video-01.mp4"]       # upload to PixelCloud, reference the links
teaser = "./videos/teaser.mp4"           # ~10 sec highlight video (PixelCloud)
starting-commit-hash = "<hash>"          # repo commit before this task's work began
